*solomon.txt*    Claude Code integration for Neovim

Author: Aiden
License: MIT

==============================================================================
CONTENTS                                                    *solomon-contents*

  1. Introduction ......................... |solomon-introduction|
  2. Requirements ......................... |solomon-requirements|
  3. Installation ......................... |solomon-installation|
  4. Configuration ........................ |solomon-configuration|
  5. Commands ............................. |solomon-commands|
  6. Keymaps .............................. |solomon-keymaps|
  7. Actions .............................. |solomon-actions|
  8. MCP Server ........................... |solomon-mcp|
  9. Sessions ............................. |solomon-sessions|
  10. Git Integration ..................... |solomon-git|
  11. Statusline .......................... |solomon-statusline|
  12. Health Check ........................ |solomon-health|

==============================================================================
1. INTRODUCTION                                         *solomon-introduction*

Solomon is a Neovim plugin that deeply integrates Claude Code CLI into your
editor. It provides a hybrid architecture combining terminal embedding with
an MCP bridge for bidirectional communication.

Features:
- Claude Code CLI in a side panel or floating window
- Code actions (explain, improve, task, ask) in normal and visual mode
- Inline replace with animated spinner for improve and task actions
- Streaming response display with code block apply
- MCP server with permessage-deflate compression for full IDE integration
- Send files/selections to Claude as @mentions
- Session management with picker
- Git integration (diff review, commit messages, blame)
- Lualine statusline component
- Auto-includes surrounding context, CLAUDE.md, and LSP diagnostics

==============================================================================
2. REQUIREMENTS                                         *solomon-requirements*

Required:~
- Neovim >= 0.11.0
- Claude Code CLI (`claude`) in PATH
- nui.nvim (prompt window UI)

Optional:~
- snacks.nvim — terminal, picker, notifications
- lualine.nvim — statusline component
- which-key.nvim — keymap discovery
- gitsigns.nvim — git hunk context
- libz (system zlib) — WebSocket compression (auto-detected)

==============================================================================
3. INSTALLATION                                         *solomon-installation*

With lazy.nvim:~
>lua
  {
    "your-username/solomon.nvim",
    dependencies = {
      "MunifTanjim/nui.nvim",
    },
    opts = {},
    keys = {
      { "<leader>an", desc = "Solomon: Open Claude Code" },
      { "<leader>aa", desc = "Solomon: Send to Claude" },
    },
  }
<

Local development:~
>lua
  { dir = "/path/to/solomon", opts = {} }
<

==============================================================================
4. CONFIGURATION                                       *solomon-configuration*

Call `require("solomon").setup(opts)` with any of these options:
>lua
  require("solomon").setup({
    terminal = {
      style = "split",          -- "float" or "split"
      float_opts = {
        width = 0.8,
        height = 0.8,
        border = "rounded",
      },
      split_opts = {
        position = "right",
        size = 0.4,
      },
      auto_close = true,
      auto_insert = true,
    },
    keymaps = {
      send = "<leader>aa",
      toggle = "<leader>an",
      focus = "<leader>af",
      close = "<leader>aq",
      ask = "<leader>ak",
      explain = "<leader>ae",
      improve = "<leader>ai",
      task = "<leader>at",
      sessions = "<leader>as",
      continue_session = "<leader>ac",
      diff = "<leader>ad",
      commit = "<leader>am",
      blame = "<leader>ab",
    },
    cli = {
      cmd = "claude",
      model = nil,
      args = {},
    },
    mcp = {
      enabled = true,
      auto_start = true,
    },
  })
<

Set any keymap to `""` to disable it.

==============================================================================
5. COMMANDS                                                 *solomon-commands*

:Solomon [subcommand]                                             *:Solomon*

Terminal:~
  `:Solomon`              Open new Claude Code terminal
  `:Solomon open`         Open new Claude Code terminal
  `:Solomon focus`        Focus existing Claude Code terminal
  `:Solomon close`        Close Claude Code terminal

Context:~
  `:Solomon send`         Send selection/file to Claude as @mention

Git:~
  `:Solomon diff`         Review unstaged git diff
  `:Solomon diff-staged`  Review staged git diff
  `:Solomon diff-hunk`    Review current git hunk
  `:Solomon commit`       Generate commit message from staged changes
  `:Solomon blame`        Explain git blame for current line

Sessions:~
  `:Solomon sessions`     Browse all sessions (picker)
  `:Solomon sessions-project` Browse sessions for current project
  `:Solomon continue`     Continue most recent session
  `:Solomon resume [id]`  Resume session by ID, or open picker

MCP:~
  `:Solomon mcp-start`    Start the MCP server
  `:Solomon mcp-stop`     Stop the MCP server
  `:Solomon mcp-status`   Show MCP server status
  `:Solomon mcp-log`      Open MCP debug log file

==============================================================================
6. KEYMAPS                                                   *solomon-keymaps*

Default keymaps (all under `<leader>a` prefix):

All code actions work in both normal and visual mode. In normal mode,
treesitter selects the enclosing function/method/block.

Code actions:~
  `<leader>ae`   Explain code (popup response)
  `<leader>ai`   Improve code — fix, refactor, optimize (inline replace)
  `<leader>at`   Task — custom prompt, inline replace
  `<leader>ak`   Ask Claude — free-form question (popup response)

Terminal & context:~
  `<leader>an`   Open new Claude Code terminal
  `<leader>af`   Focus existing Claude Code terminal
  `<leader>aq`   Close Claude Code terminal
  `<leader>aa`   Send selection/file/neo-tree file to Claude as @mention
  `<leader>ac`   Continue last session
  `<leader>as`   Browse sessions

Git:~
  `<leader>ad`   Git diff review
  `<leader>am`   Generate commit message
  `<leader>ab`   Explain git blame

Response window:~
  `q`/`<Esc>`    Cancel and close
  `a`            Apply code block to source buffer
  `y`            Yank code block to clipboard
  `d`            Open diff view of code block vs original

Diff view:~
  `<CR>`         Accept proposed changes
  `q`            Reject and close diff
  `<Esc>`        Close diff

Prompt window:~
  `<CR>`         Submit prompt (normal mode)
  `<C-CR>`       Submit prompt (insert mode)
  `<Esc>`        Cancel
  `<Tab>`        Switch between context and input panes

==============================================================================
7. ACTIONS                                                   *solomon-actions*

Actions send code to Claude and display the result either inline (replacing
the selection) or in a popup response window.

Inline actions (spinner in buffer, auto-replace):~
  `improve`  — Fix bugs, refactor, and optimize in one pass
  `task`     — Custom instruction with prompt window first

Popup actions (streaming markdown response):~
  `explain`  — Explain the code
  `ask`      — Free-form question with code context

All actions auto-include:
- Surrounding context (treesitter enclosing function or 15-line fallback)
- Project conventions from CLAUDE.md
- LSP diagnostics for the selected range

Actions use `claude -p --output-format stream-json --verbose` for streaming.

==============================================================================
8. MCP SERVER                                                   *solomon-mcp*

Solomon runs a WebSocket MCP server with permessage-deflate compression
that allows Claude Code to interact with your Neovim editor bidirectionally.

The server:~
- Starts automatically on plugin setup (configurable)
- Listens on a random localhost port
- Writes a lock file to `~/.claude/ide/<port>.lock`
- Claude Code discovers it automatically
- Uses RFC 7692 permessage-deflate compression via system zlib

MCP Tools (Claude can call these):~
  `openFile`              Open a file in the editor
  `getOpenEditors`        List all open buffers
  `getCurrentSelection`   Get current visual selection
  `getLatestSelection`    Get most recent selection
  `getDiagnostics`        Read LSP diagnostics
  `openDiff`              Open a diff view
  `closeAllDiffTabs`      Close all diff views
  `getWorkspaceFolders`   Get workspace folders
  `checkDocumentDirty`    Check for unsaved changes
  `saveDocument`          Save a document

Context sending:~
Press `<leader>aa` to send the current selection, file, or neo-tree file
to Claude as an @mention via MCP broadcast.

==============================================================================
9. SESSIONS                                                 *solomon-sessions*

Browse and resume Claude Code conversations from within Neovim.

Sessions are read from `~/.claude/history.jsonl`. The picker shows:
- Date and time
- Project directory
- First message preview

The preview panel shows the first 10 messages of the selected session.

==============================================================================
10. GIT INTEGRATION                                            *solomon-git*

Git-aware features that send context to Claude:

`:Solomon diff`~
  Sends the current `git diff` to Claude for code review.

`:Solomon diff-staged`~
  Sends `git diff --staged` for review.

`:Solomon diff-hunk`~
  Reviews just the current gitsigns hunk (requires gitsigns.nvim).

`:Solomon commit`~
  Sends staged diff and recent commit log. Claude generates a commit
  message in conventional commit format.

`:Solomon blame`~
  Runs `git blame --porcelain` on the current line or visual selection
  and asks Claude to explain the history.

==============================================================================
11. STATUSLINE                                           *solomon-statusline*

Solomon provides a lualine component showing MCP status, model, and
cumulative cost.

Add to your lualine config:~
>lua
  lualine_x = {
    require("solomon.statusline").lualine(),
  }
<

Or use the component function directly:~
>lua
  lualine_x = {
    {
      require("solomon.statusline").component(),
      cond = require("solomon.statusline").condition(),
    },
  }
<

==============================================================================
12. HEALTH CHECK                                             *solomon-health*

Run `:checkhealth solomon` to verify:
- Claude CLI is installed and accessible
- Neovim version is compatible
- Required and optional plugins
- MCP server status
- zlib availability for WebSocket compression

==============================================================================
vim:tw=78:ts=8:ft=help:norl:
